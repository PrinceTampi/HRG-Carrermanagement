# Pages

Each route has one page entry point under `pages/public/` or `pages/admin/`. Page files own layout composition and delegate repeated UI to `components/`.

The current entries bridge to the original templates so existing output remains stable while templates are migrated one page at a time.