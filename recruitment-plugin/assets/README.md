# Assets

Keep CSS, JavaScript, images, icons, and page-specific visual resources here. Use `css/`, `js/`, and `images/` subfolders as the asset set grows. Enqueue assets from the plugin controller or a dedicated module, never from a page template.